# Reference Manager with Project Support

## Setup Instructions

1. Clone the repository:
   ```bash
   git clone <repository-url>
   python -m pip install -r requirements.txt
   python -m spacy download en_core_web_sm
